{
  "name": "training-app",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build && cp -r dist ../api/dist"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cookie-session": "^2.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  }
}
